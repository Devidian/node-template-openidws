export enum wsCodes {
	AUTH,
	USER,
};

export enum userCodes {
	SELF,
	OTHER,
};

export enum AuthTypes {
	LOCAL,
	GOOGLE,			// OIDC
	MICROSOFT,		// OIDC
	PAYPAL,			// OIDC
	SALESFORCE,		// OIDC
	YAHOO,			// OIDC
	PHANTAUTH,		// OIDC
	FACEBOOK,		// fb-connect
	STEAM,			// oid2
	TWITCH,			// OIDC
};
